# hocky-insight

# sql commands
sqlite3 instance/flaskr.sqlite
flask --app flaskr {command}

sudo systemctl reload nginx
sudo systemctl restart hocky-insight

sudo rm -rf /var/www/gymrally.com/html/*
sudo cp -r build/* /var/www/gymrally.com/html/